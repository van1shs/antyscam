<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Regulamin Serwera | Anty Scam</title>
    <style>
        :root {
            --bg-color: #121212;
            --container-bg: #1e1e1e;
            --text-color: #e0e0e0;
            --primary-color: #ff3b3b; /* Czerwony akcent bota */
            --secondary-color: #ff6b6b;
            --border-color: #333;
            --shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            background-color: var(--container-bg);
            max-width: 800px;
            width: 100%;
            padding: 40px;
            border-radius: 12px;
            box-shadow: var(--shadow);
            border: 1px solid var(--border-color);
        }

        h1 {
            color: var(--primary-color);
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 10px;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .subtitle {
            text-align: center;
            color: #888;
            margin-bottom: 40px;
            font-size: 1.1rem;
        }

        .section {
            margin-bottom: 30px;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 20px;
        }

        .section:last-child {
            border-bottom: none;
            padding-bottom: 0;
            margin-bottom: 0;
        }

        h2 {
            color: var(--secondary-color);
            font-size: 1.5rem;
            margin-top: 0;
            display: flex;
            align-items: center;
        }

        h2::before {
            content: '';
            display: inline-block;
            width: 8px;
            height: 24px;
            background-color: var(--primary-color);
            margin-right: 15px;
            border-radius: 4px;
        }

        p {
            line-height: 1.8;
            margin: 10px 0;
            color: #ccc;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            padding: 10px 15px;
            background-color: rgba(255, 255, 255, 0.03);
            margin-bottom: 8px;
            border-radius: 6px;
            border-left: 3px solid transparent;
            transition: border-left 0.3s ease;
        }

        li:hover {
            border-left: 3px solid var(--primary-color);
            background-color: rgba(255, 255, 255, 0.05);
        }

        .footer {
            text-align: center;
            margin-top: 50px;
            color: #666;
            font-size: 0.9rem;
        }

        @media (max-width: 600px) {
            .container {
                padding: 20px;
            }
            h1 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>REGULAMIN</h1>
        <div class="subtitle">Oficjalne zasady serwera Anty Scam</div>

        <div class="section">
            <h2>§1. Postanowienia ogólne</h2>
            <p>1. Każdy użytkownik dołączający do serwera akceptuje niniejszy regulamin.</p>
